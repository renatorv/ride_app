class Address {
  String placeFormatedAddress;
  String placeName;
  String placeId;
  double latitude;
  double longitude;

  Address({this.placeFormatedAddress, this.placeName, this.placeId, this.latitude, this.longitude});
}
